<% if notice %>
  <aside id="notice"><%= notice %></aside>
<% end %>

<h2>Saved Jobs</h2>
<ul>
  <% @saved.line_items.each do |item| %>
    <li><%= item.job.title %></li>
  <% end %>
</ul>
<%= link_to 'Edit', edit_saved_path(@saved) %> |
<%= link_to 'Back', saveds_path %>
